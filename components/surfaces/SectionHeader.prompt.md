# SectionHeader

Section title block with uppercase aurora `kicker`, `title`, `description`, and right `action` slot.

```jsx
<SectionHeader kicker="INSIGHT · 洞察" title="今日關注動態"
  description="橫跨理財、工作、研究的即時脈動"
  action={<Button variant="ghost" size="sm">全部</Button>} />
```
