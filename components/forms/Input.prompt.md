# Input

Text field with `label`, `icon`, `hint`, `error`, and `suffix`. Sizes `sm`/`md`/`lg`.

```jsx
<Input label="Email" icon={<Mail/>} placeholder="you@example.com" hint="每週一早晨送達" />
<Input label="金額" suffix="TWD" error="必填" />
```
