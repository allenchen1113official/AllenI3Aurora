# IconButton

Square, icon-only button. Variants `solid` / `surface` / `ghost`; sizes `sm`/`md`/`lg`. Always pass `label` for accessibility.

```jsx
<IconButton variant="ghost" active label="設定"><Gear/></IconButton>
```
