# Tag

Interactive pill for filters / topics / chips. `selected` for active state; pass `onRemove` for a × affordance.

```jsx
<Tag selected>#理財</Tag>
<Tag onRemove={() => drop(t)}>#Podcast</Tag>
```
